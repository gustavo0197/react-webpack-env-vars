import React from "react";
const title = chatConfig.TITLE;
const colour = chatConfig.COLOUR;

export default () => {
  return (
    <div>
      Root app component - {title} - {colour}
    </div>
  );
};
